export const font = {
  regular: 'font-family: var(--body-font); font-weight: normal;',
  medium: 'font-family: var(--body-font); font-weight: 500;',
  bold: 'font-family: var(--body-font); font-weight: 600;',
  size: (size: number) => `font-size: ${size}px;`,
};